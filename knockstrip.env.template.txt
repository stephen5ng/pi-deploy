# knockstrip credentials -- copy to the boot partition as knockstrip.env during
# SD card prep, the same way dietpi.txt and dietpi-wifi.txt are placed. Bootstrap
# installs it to /etc/knockstrip.env (mode 0600), which knockstrip.service already
# reads via EnvironmentFile.
#
# This exists because these values cannot be defaulted or committed, yet a reflash
# without them yields a Pi that deploys and builds firmware but cannot grant
# pursuit credit. Placing the file at imaging time is what makes a rebuild
# reproducible.
#
# Fill in real values before copying. Never commit the filled-in copy.
PUSHER_SECRET=
CLUE_STATUS_API_KEY=
VERIFY_PURSUIT_ID=
VERIFY_PHONE_NUMBER=
POSTHOG_API_KEY=
