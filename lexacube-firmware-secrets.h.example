// Copy this file to /etc/lexacube-firmware-secrets.h on the Raspberry Pi,
// replace the placeholders, and restrict it to root:
//
//   sudo install -m 600 lexacube-firmware-secrets.h.example \
//     /etc/lexacube-firmware-secrets.h
//
// Never commit the populated file.

#define SSID_NAME "primary-wifi-name"
#define WIFI_PASSWORD "primary-wifi-password"
#define SSID_NAME_PORTABLE "game-wifi-name"
#define WIFI_PASSWORD_PORTABLE "game-wifi-password"
